from .deformation.route_calculation import calculate_route

class volumeSeries:
    def __init__(self, Volumes):
        self.Volumes = Volumes
    def interpolate_series(self):
        for v in self.Volumes:
            
            
     
